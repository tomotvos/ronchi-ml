# Evaluation Plan (Regression POC)

## Metrics
- **RMSE** and **MAE** of predicted `p_corr` vs ground truth.
- Optional: residuals binned by `f`, `offset`, `lpi`.

## Definition of Done
- RMSE ≤ 0.05 on validation across your target f/# and offset ranges.
